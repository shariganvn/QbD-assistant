# Decision-package fixtures

These fixtures are generated, never hand-edited, by
`decision-package-fixtures.mjs` through the retained injected P4
`createReasoningCli({ publicationRoot })` factory. They were generated against
the retained P4 contract at `68e0ca75c009c21e6dedf7ab5d24031d664ee712` from
`cowork-p2-kit/reasoning/tests/fixtures/store/records.jsonl`.

`selected` uses the test-approved rubric pin, `inconclusive` deliberately has
no rubric pin, and `attested` carries the committed linear-attestation fixture.
Regenerate all three together with the generator and retain the receipt pins in
`docs/plans/qbd-rationale-report-layer/gates.yaml`.
