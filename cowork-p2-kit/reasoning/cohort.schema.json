{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "QbD reasoning cohort",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "cohort_id", "candidates", "provenance_candidate_map", "store_records_sha256"],
  "properties": {
    "schema_version": { "const": 1 },
    "cohort_id": { "type": "string", "minLength": 1 },
    "candidates": { "type": "array", "minItems": 1, "items": { "type": "string", "minLength": 1 }, "uniqueItems": true },
    "provenance_candidate_map": { "type": "object", "additionalProperties": { "type": "string", "minLength": 1 } },
    "store_records_sha256": { "type": "string", "pattern": "^[a-f0-9]{64}$" }
  }
}
