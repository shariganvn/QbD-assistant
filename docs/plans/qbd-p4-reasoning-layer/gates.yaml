schema_version: 1
workstream: qbd-p4-reasoning-layer
canonical_plan: docs/plans/qbd-p4-reasoning-layer/plan.md
status_rule: all blocking gates require status=pass and a readable evidence artifact

# Gate evidence is machine-produced only. Bare `node --test` writes no evidence file, so every
# gate command below invokes the Step 1 wrapper, which owns the evidence write.
runner:
  wrapper: cowork-p2-kit/reasoning/tests/run-gate.mjs
  gate_id_pattern: ^G-P4-0[1-5]$
  evidence_dir: docs/reports/qbd-p4-reasoning-layer/gates/
  step_close_dir: docs/reports/qbd-p4-reasoning-layer/gates/step-close/
  red_result_dir: docs/reports/qbd-p4-reasoning-layer/gates/red/
  test_reporter: tap
  node_engines: "22.x"
  suite_runner: npm run verify:reasoning
  publication_root: docs/reports/qbd-p4-reasoning-layer/decision/

# Approval pins remain human/FD-controlled. The store pin is a reproducibility baseline computed in
# Step 0 from the byte-identical live-source and committed fixture snapshot; repins require a reason.
pins:
  selection_rubric_approved_sha256: null      # deferred, owner FD; see plan.md Deferred work
  selection_rubric_approved_pinned_by: null   # human commit author + date
  linear_attestation_sha256: null             # deferred, owner FD
  linear_attestation_pinned_by: null          # human commit author + date
  store_records_sha256: 6a16599838b8335e58f4e4f985c78d089cdd55e1a9b11696d240414b2fc28c56  # Step 0 byte-identical source/snapshot SHA-256
  store_records_repin_reason: null            # required whenever store_records_sha256 changes

gates:
  - id: G-P4-01
    name: contract-and-harness-freeze
    requirement: Layer B artifact envelopes are exact-key, fail-closed, and validated before any publication, and the gate harness itself is deterministic.
    invariant: Unknown keys, missing required fields, non-canonical artifacts, and unbound fact cards are rejected with a stable string error code; a failed validation leaves every previously published artifact byte-identical.
    boundary: cowork-p2-kit/reasoning/*.schema.json, contracts.mjs, errors.mjs, cli.mjs, publication.mjs, and the retained reasoning gate harness
    fixture: cowork-p2-kit/reasoning/tests/fixtures/contract/ with version-2 non-attested and attested cohort/decision branches, a version-2 cross-strength linear attestation, an exact evidence-log v2 plus a rejected v1 evidence-log, retained version-1 unrelated envelopes, invalid binding variants generated in tests, and the captured TAP epilogue sample for the wrapper contract test
    command: [node, cowork-p2-kit/reasoning/tests/run-gate.mjs, G-P4-01, cowork-p2-kit/reasoning/tests/contract.test.mjs, cowork-p2-kit/reasoning/tests/output-preservation.test.mjs, cowork-p2-kit/reasoning/tests/run-gate-contract.test.mjs]
    expected:
      exit_code: 0
      assertions:
        - the reopened linear-attestation, cohort, and decision validators require schema version 2 and reject version 1, unknown keys, missing required fields, and wrong types with their declared E_ codes; unrelated artifact validators remain on version 1
        - the CLI exits nonzero and prints a stable E_ string code on stderr; exit status is 1 for every failure category, matching the Layer A and Layer C convention
        - SHA-256 hashes of every file in an injected output root are equal before and after each failing CLI run
        - one shared serializer recursively sorts object keys, preserves array order, emits two-space-indented JSON plus exactly one LF, and supplies both publication bytes and the bytes hashed for linear_attestation_sha256
        - a valid run publishes canonical JSON through temp-file plus rename, and the CLI refuses any publication path outside the declared publication root
        - a fact card is rejected unless all four bindings hold - (a) its candidate resolves through the committed provenance.file to candidate map and a numeric measure card cites only records whose provenance file maps to that candidate, (b) the card's literal value and unit token appear inside its quote string, (c) content.slice(char_start, char_end) equals the quote exactly, (d) the quote meets the declared minimum length - each violation carrying its own E_ code
        - the linear-attestation envelope contains at least two unique candidate/strength members spanning at least two strengths plus common API, dosage form, product target, and trial context; incomplete scope and duplicate pairs are rejected with the declared stable E_ code, while requested-set and candidate-profile matching remain in G-P4-02
        - the cohort and decision envelopes carry nullable linear_attestation_id and linear_attestation_sha256 plus a non-empty cohort_basis; ID/hash are both null or both non-null
        - before publication, cohort and decision agree on cohort_id, attestation ID/hash, and cohort basis; a non-null ID matches the supplied attestation and a non-null hash matches its canonical bytes, otherwise E_REASONING_ARTIFACT_BINDING is emitted and all prior output bytes remain unchanged
        - evidence-log version 2 has exact top-level keys schema_version, cohort_id, entries, and exclusions; each admitted-record entry retains record_id, candidate, a unique sorted fact_card_ids array, quote, and provenance file/page/char_start/char_end; each named exclusion has exact scope/candidate/nullable-record_id/reason fields and a stable E_ reason
        - evidence-log entries and exclusions use deterministic ordering; v1, unknown or missing keys, invalid scope/nullability/order, duplicate fact-card IDs, invalid page/offsets, and non-E_ reasons fail with E_EVIDENCE_LOG_ENVELOPE
        - before publication, evidence-log cohort_id agrees with the cohort; every entry binds exactly to `record.provenance` file/page/char_start/char_end/quote, the candidate map, and referenced fact cards; Layer B does not slice segment-local record.content with Layer-A page-global provenance offsets; mismatches fail with E_REASONING_ARTIFACT_BINDING and preserve all prior output bytes
        - the gate evidence record carries the Step 0 store fixture SHA-256 and it equals pins.store_records_sha256
        - the shared version() helper, run-gate wrapper, package.json, fact-card/rubric contracts, cohort schema, Step 0 fixture, and store SHA pin remain unchanged; the retained wrapper still accepts only gate IDs matching ^G-P4-0[1-5]$, forwards multiple test paths, forces TAP, and parses the TAP epilogue
    evidence: docs/reports/qbd-p4-reasoning-layer/gates/G-P4-01.json
    owner_step: 1
    blocks: [G-P4-02, G-P4-03, G-P4-04, G-P4-05]
    status: pass

  - id: G-P4-02
    name: cohort-and-evidence-boundary
    requirement: Only compatible candidates and FD-selected MVP evidence can enter a comparison, and candidate identity comes from a committed map rather than agent assertion.
    invariant: Candidates differing in API, dosage form, product target, or trial context never enter one comparison cohort. Strength-specific candidates enter one cohort only when the exact candidate/strength set is covered by a complete, hash-pinned FD linear-formulation attestation. The MVP invocation record set is the selected package boundary; classification, citable, and deferred document-control fields do not gate records inside it. This gate forms eligible cohorts but does not score or rank them.
    boundary: cohort assignment and evidence admission modules under cowork-p2-kit/reasoning/
    fixture: cowork-p2-kit/reasoning/tests/fixtures/store/ - keep the pinned F-01/F-02 (5 mg) and F-03 (10 mg) snapshot byte-identical; add a separate selected-internal-package fixture, low-confidence and needs-OCR negatives, complete/incomplete/scope-mismatched test attestations with matching/missing/mismatched test pins, wrong candidate/formulation identity, and unbound quotes. The 2026-07-24 public/mock gate evidence is historical only.
    command: [node, cowork-p2-kit/reasoning/tests/run-gate.mjs, G-P4-02, cowork-p2-kit/reasoning/tests/cohort-evidence.test.mjs]
    expected:
      exit_code: 0
      assertions:
        - without a complete attestation, F-03 at 10 mg is excluded from the F-01/F-02 5 mg cohort and the evidence log records the explicit named reason; the cohort artifact carries only the admitted candidate set and cohort basis
        - an incomplete, scope-mismatched, unpinned, or hash-mismatched linear attestation is rejected; a complete test-only attestation with its matching test pin produces one cohort containing all and only its declared candidate/strength pairs and retains the attestation ID and SHA-256
        - selected internal/internal-derived records, including citable false and absent document-control metadata, are admitted when their deterministic integrity checks pass; low-confidence, needs-OCR, wrong candidate/formulation identity, and unbound-quote cases are refused with named reasons
        - every admitted record retains provenance file, page, char offsets, and quote in the evidence log
        - buildCohortEvidence emits the exact evidence-log v2 contract, its output passes validateEvidenceLog, and the real CLI can publish it with the matching cohort, fact cards, and store; no private in-memory-only evidence-log shape is accepted
        - a card citing record d415da715d35741b (trial-03 Results) while claiming candidate F-01 is refused with a named E_ code, because that record's provenance file maps to F-03
        - a card asserting a value or strength absent from its own quote - for example strength "5 mg" quoting "Trial Overview" from the F-03 overview record - is refused with a named E_ code
    evidence: docs/reports/qbd-p4-reasoning-layer/gates/G-P4-02.json
    owner_step: 2
    blocks: [G-P4-03, G-P4-05]
    status: pass

  - id: G-P4-03
    name: test-only-rubric-decision-core
    requirement: The deterministic mechanism proves a selection only for an explicitly test-approved, hash-pinned fixture; every production-shaped or unpinned path is inconclusive, while Step 1/2 contracts remain unchanged.
    invariant: Missing rubric authority, missing critical evidence, unresolved conflict, unsupported normalization, uncited admitted Results evidence, tie, and sensitivity instability are explicit decision states, never score zero or invented data.
    boundary: new v2 selection-rubric and selection-evaluation contracts, decision-engine.mjs, and their isolated fixtures; completed v1 rubric/v2 decision/Step 2 artifacts are read-only upstream inputs.
    fixture: cowork-p2-kit/rubric/selection-rubric-proposal.v2.json with approval_state proposal; cowork-p2-kit/reasoning/tests/fixtures/rubric/selection-rubric-test-approved.v2.json and immutable canonical-byte selection-rubric-test-pin.json; synthetic exact record-role maps and fact-card sets for hard-gate, missing, conflict, tie, instability, unit/normalization, and uncited-results cases; retained Step 2 non-attested and test-attested cohorts.
    command: [node, cowork-p2-kit/reasoning/tests/run-gate.mjs, G-P4-03, cowork-p2-kit/reasoning/tests/decision-engine.test.mjs]
    expected:
      exit_code: 0
      assertions:
        - a v2 proposal, missing pin, and mismatched pin return an inconclusive v2 decision with winner null and respectively E_RUBRIC_APPROVAL_REQUIRED, E_RUBRIC_PIN_REQUIRED, and E_RUBRIC_PIN_MISMATCH; none evaluates a score or winner
        - only the committed test-approved fixture with its immutable matching test pin may select a synthetic winner; its decision embeds the applied canonical rubric SHA-256, while pins.selection_rubric_approved_sha256 remains null and is never read or repinned by the test
        - every rubric measure has exact declared raw shape, aggregation, unit, hard gate, score map, weight range, and conflict tolerance; unsupported unit/raw grammar or a normalized_value inconsistent with raw_text returns its mapped inconclusive code rather than a conversion or score
        - every matrix cell carries derived value/unit plus sorted fact-card and admitted record IDs; the hash-bound evaluation sidecar records the decision ID, canonical decision SHA-256, cohort ID, applied rubric SHA-256, candidate reviews, and sensitivity result
        - hard gates run before soft scoring and remove the failed candidate from eligibility; missing critical evidence, an over-tolerance conflict, or an uncited admitted record explicitly marked results returns its mapped inconclusive outcome
        - an inside-threshold nominal margin or a leader that changes or loses its strict margin at any Cartesian min/max weight vector returns E_TIE_OR_SENSITIVITY_UNSTABLE
        - existing Step 2 non-attested and test-attested cohorts retain their cohort candidate set, attestation ID/hash, and cohort basis; this gate neither forms a cohort nor adds a cross-strength exception
        - frozen upstream contract validators (`contracts.mjs` and `errors.mjs`) remain byte-identical; `cli.mjs` and `publication.mjs` are explicitly reserved Step 4 extension seams, whose retained legacy behavior is rechecked by G-P4-01 and new package behavior by G-P4-04; machine gate evidence is regenerated per run
        - no Markdown, CLI publication, receipt, or execution report is created or asserted by G-P4-03; those behavior and artifact-boundary checks remain exclusively in G-P4-04/05
    evidence: docs/reports/qbd-p4-reasoning-layer/gates/G-P4-03.json
    owner_step: 3
    blocks: [G-P4-04, G-P4-05]
    status: pass

  - id: G-P4-04
    name: skill-and-artifact-publication
    requirement: The rewritten Cowork skill publishes only a fully validated Step 2/3 decision package, cannot widen the current run, cannot hand-author derivatives, and keeps its human-only execution report outside every decision path.
    invariant: formula-decision.md regenerates from the hash-bound formula-decision.json plus selection-evaluation.json pair; evidence-log.md regenerates from evidence-log.json; a receipt hashes every package member; neither evidence outside the explicitly supplied package nor P.2.2/P.2.3 drafting can enter this workstream's output; and a human-only execution report is never auto-loaded into a later agent's context.
    boundary: cowork-p2-kit/SKILL.md, execution-report.mjs, markdown.mjs, publication-receipt.{schema.json,mjs}, the publish-package CLI path, and publishDecisionPackage()
    fixture: the SKILL.md fact-card template in its declared fenced info-string tag, a valid Step 2 package plus frozen Step 3 decision/evaluation pair, an evidence sample absent from the explicitly supplied package, a selected internal-record sample, decision/evaluation ID/SHA/cohort/rubric binding mismatches, a hand-authored formula-decision.md that agrees with its inputs, a surplus-content variant carrying non-contradicting instruction-shaped text, receipt hash/missing/stale-attestation variants, and a human-only execution-report fixture carrying attempted instruction-shaped text
    command: [node, cowork-p2-kit/reasoning/tests/run-gate.mjs, G-P4-04, cowork-p2-kit/reasoning/tests/skill-artifacts.test.mjs]
    expected:
      exit_code: 0
      assertions:
        - every artifact template extracted from SKILL.md's declared fenced blocks validates against its committed schema
        - publish-package accepts the complete Step 2 package plus the frozen Step 3 decision/evaluation pair; decision/evaluation ID, canonical decision SHA-256, cohort ID, rubric SHA-256, candidate/admitted-record, and existing Step 1/2 bindings all validate before staging any package byte
        - formula-decision.md is generated deterministically from canonical formula-decision.json plus selection-evaluation.json, and evidence-log.md from canonical evidence-log.json; the consistency check is regeneration equality, so an agreeing but hand-authored Markdown file is rejected wholesale
        - evidence absent from the explicitly supplied package cannot appear in a decision artifact or evidence log
        - the skill treats only the explicitly supplied invocation records as the selected MVP package, permits selected internal records without classification/citable/document-control gating, and forbids package widening, claims of machine-verified FD authorization, and external egress
        - SKILL.md contains none of the pinned forbidden tokens - EN "P.2.2", "P.2.3", "headings[]", "prose[]", "tables[]", "citations[]"; VN "soạn thảo", "soạn nội dung", "dự thảo" - this denylist is best-effort by declaration, and FD/human review is the authority for semantic drafting drift
        - the rewritten SKILL.md still contains the untrusted-extracted-text rule; the gate asserts its presence alongside the drafting-absence check
        - every evidence-log quote is rendered inside the declared qbd-verbatim-quote delimiters with HTML escaping for &, <, and >; no renderer accepts agent-authored Markdown
        - the publication path writes only inside docs/reports/qbd-p4-reasoning-layer/decision/ and refuses any other destination
        - a successful package has exactly the declared allowlisted members, with selection-evaluation.json and a canonical publication-receipt.json; the receipt input-store SHA equals the supplied store/cohort pin and every listed member SHA equals its on-disk bytes
        - the package publisher and published-package re-validation recompute the supplied raw-store SHA-256, recheck fact-card and evidence-log store/provenance bindings, and reject a forged receipt paired with false evidence; every surplus non-regular directory entry, including a symlink or directory, fails with E_PUBLICATION_SURPLUS_FILE
        - execution-report.mjs writes a per-run Markdown ledger only under ~/.codex/artifacts/<project>/reasoning-execution-reports/; the report has a human-only/untrusted header, contains only the permitted operational fields, and rejects record content, quotes, prompts, credentials, hidden reasoning, and instruction-shaped payloads
        - no execution report is accepted by cli.mjs, decision/evidence schemas or validators, publication transaction, publication receipt, or deterministic decision/evidence Markdown generator; an execution-report file inside the publication root makes published-artifact re-validation fail as an unrecognized surplus file
        - SKILL.md makes report discovery and reading forbidden by default, forbids report references in session state/handoffs/prompts, and permits reading only after a human supplies the exact report path or run ID; the test asserts these rules and that report text cannot be promoted into the decision inputs
    evidence: docs/reports/qbd-p4-reasoning-layer/gates/G-P4-04.json
    owner_step: 4
    blocks: [G-P4-05]
    status: pass

  - id: G-P4-05
    name: integrated-product-acceptance
    requirement: Phase 4 product acceptance requires the ordered suite, an end-to-end fixture, and re-validated published artifacts.
    invariant: Every product requirement maps to a fixture, command, raw result, and retained evidence; published artifacts agree with the pinned Step 0 input snapshot when re-checked.
    boundary: the verify:reasoning runner, the gate evidence validator, and the published artifacts under docs/reports/qbd-p4-reasoning-layer/decision/
    fixture: cowork-p2-kit/reasoning/tests/e2e-decision.test.mjs driving both policy branches - a 5 mg decision over F-01/F-02 with F-03 separate when no attestation exists, and a test-attested F-01/F-02/F-03 combined cohort/common ranking - plus a selected internal-package case, the G-P4-01 through G-P4-04 evidence records, and a tampered published-artifact variant whose receipt hash no longer matches
    command: [npm, run, verify:reasoning]
    expected:
      exit_code: 0
      assertions:
        - the runner re-runs G-P4-01 through G-P4-04 and the e2e gate in declared order through the run-gate wrapper, stopping at the first failure, so G-P4-05.json is machine-produced like the other four
        - it validates every retained gate evidence record for status pass, canonical schema, and one shared suite UUID
        - the end-to-end fixture ranks only F-01 against F-02 and records F-03 as a separate cohort
        - the test-attested branch ranks the exact F-01/F-02/F-03 scope together, and the decision/Markdown name and hash the attestation; removing or changing the attestation prevents cross-strength ranking
        - the selected internal-package branch is not rejected for classification, citable, or absent document-control metadata, while candidate/quote/extraction-quality controls remain enforced
        - formula-decision.md regenerates byte-identically from formula-decision.json plus the hash-bound selection-evaluation.json, and evidence-log.md regenerates byte-identically from evidence-log.json
        - cli.mjs package validation is re-run over the actual published fixture artifacts in docs/reports/qbd-p4-reasoning-layer/decision/ against the Step 0 committed store snapshot - decision/evaluation binding, quote containment, char-offset equality, admissibility, candidate map, applied rubric hash, and input-store hash
        - the publication validation receipt is present and every listed artifact SHA-256 matches the on-disk bytes; a missing receipt, stale optional attestation, unrecognized surplus file, or any hash mismatch fails closed
        - the committed store snapshot SHA-256 equals pins.store_records_sha256 and the value recorded in G-P4-01 evidence; a changed baseline fails unless pins.store_records_repin_reason is populated
    evidence: docs/reports/qbd-p4-reasoning-layer/gates/G-P4-05.json
    owner_step: 5
    blocks: []
    status: pass
