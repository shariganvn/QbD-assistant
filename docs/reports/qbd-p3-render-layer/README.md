# Phase 3 Render Evidence

This directory retains evidence referenced by `docs/plans/qbd-p3-render-layer/gates.yaml`.

G-P3-01 through G-P3-05 machine evidence is retained at `gates/G-P3-01.json` through
`gates/G-P3-05.json`. The G-P3-05 manual viewer-fidelity record is
`gates/G-P3-05-viewer.md`; it identifies the reviewed DOCX and records PASS for visible footnotes,
the approved USP link, local plain provenance, the static TOC, and four-column table layout.
G-P3-06 review evidence is retained in `code-review.md`.
Gate status is authoritative only in the canonical `gates.yaml`; this index must not be used to
infer a PASS result beyond the named evidence.
