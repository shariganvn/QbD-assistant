---
title: "Placeholder template ingest probe evidence"
date: "2026-08-04"
status: "rework"
scope: "Phase 02 + Phase 05 closeout; Phase 03/04 prework only"
---

# Placeholder template ingest probe evidence

## Decision

V3 INPUT CONTRACT UPDATED / HOLD PROMOTION. Phase 02 field-map compilation and
the Phase 05 evidence review are closed for this session. Phase 03 and Phase 04
were not executed; their prework remains pickup material. Promotion remains
held until the isolated two-run workflow and downstream boundaries are
independently verified with truthful provenance.

## Evidence

| Check | Result | Evidence |
|---|---|---|
| Frozen source identity | pass | V3 template SHA-256 `c492532054d9ba04d2dbd5c3d03706c423534cce5329d2657b2588760e0087e0`; public mock SHA-256 `01fe95607f4733e2b47a4c46f8dad5817d6014cc40f69a08631977c9d890cd8f` |
| Intake contract | pass | `node cowork-p2-kit/template-probe/intake/verify-intake-contract.mjs`; 146 anchors (143 cell, 3 paragraph) |
| Field-map compilation | pass | Focused suite 8/8; map has 146 entries and unique occurrence IDs |
| Map determinism | pass | V3 map generated twice with canonical hash `3f0f5120d345e182e868f65720a2656a7e061346e75ca8f9636cbd7bab41761b` |
| Receipt determinism | prework pass | V3 mock receipt generated twice with canonical hash `d81700eadb5823b519df115f1945c9d48e11ec2a0926bfc22ffd738f2ce9a4c1`; not Phase 03 execution evidence |
| Synthetic extraction | prework pass | Focused extractor suite 3/3; exact raw values, occurrence joins, deterministic receipts, fail-closed page provenance, and receipt-schema rejection; not Phase 03 execution evidence |
| Workflow boundary | prework pass | Focused workflow suite 1/1; successful extraction publishes the isolated receipt and leaves canonical inputs/store unchanged; not Phase 04 execution evidence |
| Existing ingest contracts | pass | Record-contract and repository-boundary suites 6/6 |
| Authorized mock extraction | prework pass | V3 map and mock share paragraph owners 1 and 6; 146 occurrences extract exactly, including `EXPERIMENT-DESCRIPTION = "Khảo sát tỷ lệ tá dược rã"` and `BATCH-SIZE = "1.000 viên"`; not Phase 03 execution evidence |
| Downstream probe | not tested | No truthful receipt/record projection exists for the authorized mock, so isolated ingest/reasoning/render processing of this probe would be misleading |

Rows marked `prework pass` are preparatory evidence only. They do not change
the plan status: Phase 03 and Phase 04 remain pending and not executed.

V3 removes the prior paragraph-owner and literal-boundary drift in the prework
adapter. That adapter uses exact template literals and publishes only the
isolated receipt. The existing committed record projection remains unavailable
because cell owners do not provide the page provenance required by the record
schema.

## Regression-suite boundary

Prior prework ran `npm run verify:ingest` and the existing reasoning
verification. The render suite's remaining tests passed (48/48); its one
unrelated baseline failure is the existing `isolated-network.test.mjs`
assertion that the root `plans/` directory must not exist, while this session's
pre-existing untracked `plans/` directory does exist. These suites are
regression evidence only, not proof that the blocked mock reached those
downstream branches, and they were not Phase 03/04 execution in this closeout.

## Review disposition

The mandatory code review returned `DONE_WITH_CONCERNS` with low blast radius:
no production imports reference `template-probe`, and existing ingest/store
contracts are unchanged. The review findings on nested malformed delimiters,
metadata authority, receipt schema shape, and non-atomic writes were corrected
and covered by the focused tests. The remaining concerns are intentional phase
boundaries: the full Phase 04 runner is not present because the authorized mock
is blocked, and output-root confinement remains a caller-supplied isolated-run
responsibility rather than a production publication path.

## Follow-up gates

1. Run the isolated ingest twice using the v3 hash-bound map, grammar, and
   metadata catalog.
2. Compare receipt/map/store hashes and preserve exact owner/value round-trips.
3. Review downstream branches only when a truthful projection exists, then
   compare record/store hashes and explicitly authorized downstream outputs.

## Retention and rollback

The source DOCX files remain immutable, public/synthetic, and excluded from the
canonical ingest manifest. The map, contracts, tests, receipt code, and this
stateful report remain isolated probe material and are not production authority.
Rollback requires removing only the probe adapter and ignored derived artifacts;
the committed record schema, canonical inputs, and canonical store require no
restoration.
