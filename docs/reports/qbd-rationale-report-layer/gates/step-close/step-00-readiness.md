# Source fixture readiness

Validated 2026-07-29 with the retained P4
`validatePublishedDecisionPackage()` and the pinned source store:

```text
selected: valid
inconclusive: valid
attested: valid
```

The generated fixture provenance test also compares every fixture byte to fresh
output from the injected P4 CLI factory.
