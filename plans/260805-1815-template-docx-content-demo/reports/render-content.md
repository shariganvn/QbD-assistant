# Rendered content

Date: 2026-08-06

Command:

```text
node --test cowork-p2-kit/workflow-trial/tests/content-demo.test.mjs
```

Result: pass, 5/5 tests.

The retained review artifact is:

[p2-draft.docx](/media/E/VIBECODING/MODULE3-agent/artifacts/template-docx-content-demo/run-msgvldj7/p2-draft.docx)

Raw DOCX SHA-256:

```text
2a47ab07b52f6e1fa0ccd7f56b43a7b360d97fe5e7d9428068fd009ee6c00b95
```

The render receipt proves Bubblewrap executed with `--unshare-net`, exit code
0, Node `v22.22.2`, npm `10.9.7`, and Bubblewrap `0.11.1`. There is no
unsandboxed render fallback.

The citation source path is the canonical normalized mock path
`inputs/trials/placeholder-probe/filled-public-mock-document-030826.docx`.
Reopened DOCX text begins with the exact synthetic watermark and places it
before `Mục lục`. The rendered body includes the representative heading, two
paragraphs, the score table, and the Sources/provenance section with exactly
three citation entries. No `⏳ Chờ dữ liệu` text is present.
